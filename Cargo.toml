[package]
name = "qrow"
version = "0.1.0"
edition = "2024"
description = "A native SQL workbench for Spark through Kyuubi"
license = "Apache-2.0"
default-run = "qrow"

[features]
default = ["ui"]
ui = ["dep:gpui", "dep:gpui-component", "dep:gpui-component-assets", "dep:tree-sitter-sequel", "dep:async-channel"]

[[bin]]
name = "qrow"
path = "src/main.rs"
required-features = ["ui"]

[dependencies]
anyhow = "1"
async-channel = { version = "2", optional = true }
gpui = { version = "=0.2.2", default-features = false, features = ["runtime_shaders"], optional = true }
gpui-component = { version = "=0.5.1", optional = true }
gpui-component-assets = { version = "=0.5.1", optional = true }
tree-sitter-sequel = { version = "0.3.8", optional = true }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
thrift = "0.24"
uuid = { version = "1", features = ["v4", "serde"] }
zeroize = "1"

[target.'cfg(target_os = "macos")'.dependencies]
security-framework = "3"

[dev-dependencies]
tempfile = "3"
proptest = "=1.11.0"

[profile.release]
lto = "thin"
strip = true

[lints.rust]
unsafe_code = "forbid"
unused_must_use = "deny"

[lints.rustdoc]
broken_intra_doc_links = "deny"
bare_urls = "warn"

[lints.clippy]
all = { level = "warn", priority = -1 }
await_holding_lock = "deny"
dbg_macro = "deny"
todo = "deny"
unimplemented = "deny"
mem_forget = "deny"
large_stack_arrays = "warn"
large_futures = "warn"
needless_collect = "warn"
redundant_clone = "warn"
inefficient_to_string = "warn"

[[bench]]
name = "sql"
harness = false
